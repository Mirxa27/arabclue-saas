# /test-suite

Run the standard verification loop:

```bash
npm run typecheck
npm test
npm run build
```

Report pass/fail for each step with the first error snippet on failure. Do not claim green without command output.
