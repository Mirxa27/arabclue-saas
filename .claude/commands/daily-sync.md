# /daily-sync

Morning briefing for arabclue:

1. Read `data/daily-logs/` for the latest log and `data/projects/arabclue.md` for current milestone.
2. Run `git status` and summarize uncommitted work vs last session goals.
3. Check `data/inbox/` for untriaged items (if any).
4. List blockers, today's priorities, and which agent (@dev, @ops, etc.) owns each.
5. Append a dated section to `data/daily-logs/<YYYY-MM-DD>.md` (append-only; do not edit past days).

If production is in scope, note deploy target (Vercel vs Hostinger) and whether `/api/health` was checked recently.
